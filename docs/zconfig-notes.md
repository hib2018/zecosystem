# zconfig Investigation Notes

`zconfig` is currently clean and contains only project-local Spec Kit skills plus zconfig-specific docs/contracts.

Keep in `zconfig` for now:

- proposal/revision/final/apply JSON Schemas
- process protocol and external validator contract
- security and redaction rules specific to configuration changes
- agent integration details for registered zconfig agents and validators

Potential future global extraction candidates:

- one-shot process invocation principles
- stable structured error-code conventions
- human final-confirmation capability boundaries
- redaction and secret-sharing principles that also apply to ztasks or zintent
- zconfig's role as an optional final configuration review boundary in the artifact flow
